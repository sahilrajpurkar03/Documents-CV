# Job Search Tool — Python dependencies

# Core job scraping (LinkedIn, Indeed, Google Jobs, StepStone, Xing)
python-jobspy>=1.1

# HTTP + HTML parsing (StepStone & Xing scrapers)
requests>=2.34
beautifulsoup4>=4.14

# Web server
flask>=3.1

# Terminal UI
rich>=15.0

# Data handling (used by jobspy internally)
pandas>=2.3
